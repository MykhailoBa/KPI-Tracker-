import SwiftUI

struct LastKPIView: View {
    var body: some View {
        VStack {
            Text("Останній KPI: 91.6%")
                .font(.largeTitle)
                .padding()
            
            Spacer()
        }
        .navigationTitle("Останній KPI")
    }
}
