import SwiftUI

struct CalendarView: View {
    var body: some View {
        VStack {
            Text("Календар змін")
                .font(.largeTitle)
                .padding()
            
            Spacer()
        }
        .navigationTitle("Календар")
    }
}
